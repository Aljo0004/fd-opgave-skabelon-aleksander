---
import ValuesContainer from "../components/ValuesContainer.astro";
---

<section class="full-bleed">
  <div class="width">
    <h3>GROWING WITH OUR CLIENTS</h3>
    <h2 class="expect">Our core values & principles</h2>
    <div class="text-button-flex">
      <p>Capitalize on low hanging fruit to identify a ballpark value added activity beta test. Override the digital divide with additional from DevOps.</p>
      <a href="#0" class="button-secondary">Get in touch</a>
    </div>
  </div>

  <ValuesContainer />
</section>

<style>
  section {
    background-color: #f5f5f5;
    padding-bottom: var(--space-2xl);
  }
  .width {
    width: 80%;
    margin: auto;
    padding-top: var(--space-2xl);
  }
  .expect {
    font-size: 4rem;
    margin-bottom: var(--space-m);
  }
  .width h2,
  .width h3,
  .width p {
    color: var(--bg-black);
    margin-bottom: var(--space-s);
  }
  .text-button-flex {
    display: flex;
    flex-direction: row;
    justify-content: space-between;
    align-items: center;
    gap: var(--space-s);
  }
  .text-button-flex a {
    display: inline-block;
    padding: 0.75em 2em;
    background: var(--bg-black);
    color: var(--white);
    border: none;
    border-radius: 32px;
    text-decoration: none;
    transition:
      background 0.2s,
      color 0.2s;
    cursor: pointer;
  }
  .text-button-flex a:hover {
    background: var(--yellow);
    color: var(--bg-black);
  }
  .text-button-flex p {
    width: 60%;
  }
  section * {
    color: var(--white);
  }
  @media (max-width: 800px) {
    .text-button-flex {
      flex-direction: column;
      align-items: flex-start;
    }
    .text-button-flex p {
      width: 100%;
    }
      .expect {
    font-size: 3rem;
    margin-bottom: var(--space-m);
  }
  }
</style>
